@using TechShop.Models
@using X.PagedList.Mvc.Core
@using X.PagedList.Web.Common;
@model X.PagedList.IPagedList<TTaiKhoan>
@{
    ViewData["Title"] = "tSanPham";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@addTagHelper *,Microsoft.AspNetCore.Mvc.TagHelpers

<div class="col-md-12">
    <div class="page_title">
        <h2>Danh mục tài khoản</h2>
    </div>
</div>
<a asp-action="Create" class="btn btn-dark text-white font-weight-bold">Create New</a>

<div class="table-responsive-sm">
    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>
                    UserName
                </th>
                <th>
                    PassWord
                </th>
                <th>Loại người dùng</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.UserName
                    </td>
                    <td>
                        @item.PassWord
                    </td>
                    <td>@item.LoaiUser</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.UserName" class="btn btn-primary btn-sm">Edit</a>

                        <a asp-action="Delete" asp-route-id="@item.UserName" class="btn btn-danger btn-sm">Delete</a>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>
<div class="row">
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            <li class="page-item">@Html.PagedListPager(Model, page => Url.Action("tSanPham", new { page = page }), new PagedListRenderOptions { LiElementClasses = new string[] { "page-item" }, PageClasses = new string[] { "page-link" } })</li>
        </ul>
    </nav>
</div>